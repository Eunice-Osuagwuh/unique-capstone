apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: taskline-backend
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: tasklineapp
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
